class sati {
  constructor(name, location) {
    this.name = name;
    this.location = location;
  }
  calcage() {
    console.log(`nan hesaru ${this.name}`);
  }
}

const S = new sati(satisha, lanka);
console.log(calcage());
